<!DOCTYPE html>
<html>
<head>
    <title>@Page.Title</title>
    <script src="~/Scripts/jquery-3.4.1.js"></script>
    <link href="~/Content/bootstrap.css" rel="stylesheet" />
    <script src="~/Scripts/bootstrap.js"></script>
    @*<link href="~/Content/myCustomStyle.css" rel="stylesheet" />*@ @*This overrides bootstraps stylesheet*@
    @*you can add any of your own customized stylesheets*@

    @*if you use this section on your content page you are specifying specific css styling for that particular page*@
    @RenderSection("head", required: false) @*required parameter means it is optional and doesn't show on every page*@
</head>
<body>
    <div class="container-fluid">
        @*A RenderSection is a secondary content block that can be  used for specific none main content body display

        We will use the "banner" RenderSection to andle the page title and menu

        RenderSection is required on each content page unless the required oaraneter has been set to false*@
        <div class="jumbotron">
            @RenderPage("~/_Menu.cshtml")
            @RenderSection("banner")
        </div>

        @*RenderBody is the main default area where your context page will be display. The RenderBody reference is physically
        removed and your context inserted*@
        @RenderBody()

        @RenderSection("footer", required: false)
    </div>
</body>
</html>
